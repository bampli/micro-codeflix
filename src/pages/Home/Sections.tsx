import SectionSlider from "../../components/Video/SectionSlider";

const Sections: React.FunctionComponent = (props) => {
    return (
        <div>
            <SectionSlider title="Documentáriooo" videos={[]} />
        </div>
    );
};

export default Sections;